#!/usr/bin/env zsh

ROOT_DIR="$(realpath "$(dirname "$0")"/..)"
VIRTUALENV="$(basename "$(basename "$ROOT_DIR")")"

PYTHONPATH="$ROOT_DIR/src" $PYENV_ROOT/versions/$VIRTUALENV/bin/python -m branches "$@"
